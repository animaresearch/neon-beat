# NEON BEAT 안정화 계획

## 목표

`server.py`와 단일 `index.html` 구조를 유지하면서, 9키 리듬게임을 긴 곡과 빠른 곡 전환에서도 멈춤·메모리 누수·오디오 어긋남 없이 동작하게 만든다.

## 현재 기준선

- 작업 경로: `E:\99_Temp_Transfer\MusicGame`
- 음원 경로: `E:\98.MUSIK`
- 런타임: Python 표준 라이브러리 + 브라우저 Web Audio/Canvas
- 변경 대상: `server.py`, `index.html`
- 보존 대상: 외부 패키지 없는 실행 방식과 단일 HTML 구조

## 확인된 P0 결함

1. 설계 키는 `W A S D SPACE J K L I`인데 구현은 `A S D F SPACE J K L ;`이다.
2. 판정은 설계의 55/110ms가 아니라 45/90ms이고, 등급 경계도 설계와 다르다.
3. 히트·미스 노트가 `activeNotes`에서 제거되지 않아 곡이 길수록 배열이 계속 커진다.
4. 판정 범위 밖의 이른 입력이 가장 가까운 노트를 즉시 Miss 처리한다.
5. 렌더 히트라인(`H-110`)과 노트 이동 기준(`H*0.88`)이 서로 달라 화면과 판정이 어긋난다.
6. `curOfs`가 선언되지 않은 암묵적 전역이다.
7. 곡을 빠르게 바꾸면 이전 fetch/decode/분석 결과가 새 선택을 덮을 수 있다.
8. 분석이 메인 스레드를 오래 점유하고 실제 진행률을 제공하지 않는다.
9. 현재 분석은 스펙트럴 플럭스가 아니라 RMS 변화량 + 영교차 추정이다.
10. 메뉴 복귀 시 선택값과 로드 상태가 어긋나 같은 곡을 다시 선택하기 어렵다.
11. 서버가 Range 요청, 잘못된 URL, 없는 음악 폴더, 클라이언트 연결 종료를 안정적으로 처리하지 않는다.

## 구현 순서

### 1단계 — 회귀 방지용 진단 훅

- 게임 설정값과 순수 판정 로직을 상수/함수로 분리한다.
- 브라우저 콘솔에서 읽기만 가능한 `window.__NEON_BEAT_DIAGNOSTICS__`를 제공한다.
- 최소 검증 항목: 키 9개, 판정 경계, 등급 경계, 노트 정리, 상태 전이.

완료 조건: 정적 구문 검사와 순수 로직 경계 검사가 통과한다.

### 2단계 — 게임 루프·입력·오디오 상태 안정화

- 모든 세션 상태를 명시적으로 초기화하고 암묵적 전역을 제거한다.
- 입력은 110ms 밖이면 무시하고, 같은 레인의 판정 가능한 가장 이른 노트만 처리한다.
- 히트/미스 노트에 제거 시각을 두어 짧은 이펙트 뒤 반드시 배열에서 제거한다.
- 하나의 히트라인 좌표를 렌더와 노트 계산이 함께 사용한다.
- `AudioContext.resume()` 완료 후 소스를 시작하며, pause/resume/end/menu 전이를 직렬화한다.
- `sourceNode.onended`와 세션 토큰으로 중복 종료를 막는다.

완료 조건: 10분 상당의 합성 노트 시뮬레이션 후 활성 배열이 유계이고 중복 판정이 없다.

### 3단계 — 로딩·곡 전환 경합 제거

- 각 로드에 증가하는 요청 토큰과 `AbortController`를 부여한다.
- 새 곡 선택, 메뉴 복귀, 재시도 시 이전 fetch/분석 결과를 폐기한다.
- 로딩 실패 시 오버레이에 재시도/메뉴 복귀 경로를 제공한다.
- START는 현재 선택 곡의 decode와 chart 생성이 모두 완료된 경우에만 활성화한다.

완료 조건: 곡을 연속으로 바꿔도 마지막 선택만 준비 상태가 된다.

### 4단계 — 자동 채보 안정화

- 모노 믹스다운 뒤 프레임별 스펙트럴 플럭스를 계산한다.
- 이동 중앙값/평균 기반 적응 임계값과 최소 간격 피크 피킹을 사용한다.
- EASY/NORMAL/HARD 밀도는 검출 피크의 45/70/95%를 목표로 하되, 과밀 구간에는 최소 간격을 둔다.
- 레인 분배는 고정 시드 PRNG + 음역 가중치 + 연속 같은 레인 억제를 사용해 재현 가능하게 한다.
- 분석을 청크로 나누고 프레임 진행률을 UI에 반영한다.
- 신호가 너무 작거나 피크가 부족한 곡에는 BPM 독립 폴백 채보를 만든다.

완료 조건: 동일 곡/난이도에서 채보가 재현되고, 무음/짧은 파일에서도 START가 영구 비활성화되지 않는다.

### 5단계 — 서버 견고성

- `ThreadingHTTPServer` 계열로 정적 파일과 음원 요청을 병렬 처리한다.
- `/api/tracks`에서 음악 폴더 부재·권한 오류를 JSON 오류로 반환한다.
- URL은 `urlsplit` 후 정확히 해석하고 파일명은 허용 목록과 `basename`/실경로 경계로 검증한다.
- 단일 byte Range 요청과 `206`, `Content-Range`, `Accept-Ranges`를 지원한다.
- `HEAD`, 올바른 MIME, `Cache-Control: no-store`(API/HTML)를 처리한다.
- BrokenPipe/ConnectionReset은 서버 크래시 없이 정리한다.

완료 조건: API, 전체 음원, 범위 음원, 404, traversal 시도가 기대 상태코드를 반환한다.

### 6단계 — UI 및 요구사항 정합성

- 키 표시를 `W A S D SPACE J K L I`로 통일한다.
- 파일 드래그앤드롭을 추가하되 서버 파일과 로컬 Blob URL의 수명주기를 분리한다.
- DPR 대응 Canvas 크기 조절과 작은 화면 레이아웃을 보강한다.
- 외부 폰트 실패 시에도 레이아웃이 유지되게 한다.
- 등급을 S 95+, A 85+, B 70+, C 55+, F로 맞춘다.

완료 조건: 메뉴 → 로딩 → 플레이 → 일시정지 → 결과 → 재시도/메뉴 흐름이 모두 동작한다.

## 검증 순서

1. `python -m py_compile server.py`
2. HTML에서 `<script>`를 추출해 JavaScript 구문 검사
3. 서버를 자동 브라우저 열기 없이 제한 시간 내 기동
4. `/api/tracks`, `/`, `/musik/...`, Range, 404, traversal HTTP 스모크 테스트
5. 브라우저 콘솔 error 0건 확인
6. 메뉴와 설정, START 활성화, ESC pause/resume, 메뉴 복귀를 브라우저에서 확인
7. 짧은 샘플 또는 첫 곡으로 decode/분석/게임 시작 확인
8. 마지막으로 실제 곡 1개를 끝까지 재생해 결과 화면과 메모리 증가를 확인

## 실행 게이트

```text
ANESTHESIA STATUS: REQUIRED (서버 시작 및 장시간 실제 곡 검증)
EXECUTION AUTHORITY: REQUIRED
PREFLIGHT=python/claude 경로|8765 포트 점유|E:\98.MUSIK 존재|대상 파일 백업/해시|정확한 명령|로그 경로|시간 제한;
WATCHDOG=서버 60초 스모크 제한|실곡 검증은 곡 길이+15초|기동한 PID만 종료|로그 보존;
RECOVERY=8765 고아 프로세스 확인|fatal console/server 오류 수집|index/server 무결성 확인;
STOP=한 번에 한 단계만 구현하고 검증 실패 시 다음 단계로 넘어가지 않음;
```

## 범위 밖

- 프레임워크 도입, 빌드 시스템 추가, 외부 Python/JavaScript 패키지 설치
- 온라인 업로드, 계정 연동, 점수 서버, 멀티플레이
- 음원의 이동·변환·삭제

